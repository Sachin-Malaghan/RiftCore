# ============================================================
# Tests — Stub (will be filled in later steps)
# ============================================================
message(STATUS "[Tests] Test suite stub registered.")